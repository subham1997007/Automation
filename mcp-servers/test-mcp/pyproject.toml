[project]
name = "test-mcp"
version = "0.1.0"
description = "Testing MCP server for TestPilot Agent"
requires-python = ">=3.10"
dependencies = ["mcp[cli]>=1.0.0", "langgraph>=0.2.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
